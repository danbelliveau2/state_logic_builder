# JARVIS Version Log

**JARVIS** is the AI L5X-generation layer of the SDC State Logic Builder:
`src/lib/agentGenerator/` (ir.js, promptBuilder.js, editPlanSchema.js, mergeEngine.js,
validator.js, client.js, generationRules.md) plus the `POST /api/generate` endpoint in `server.js`.

**Architecture (since v1.0.1 — surgical merge):** the model never writes L5X.
It authors a JSON **edit plan** against the selected SDC V4.2 standard template;
the deterministic **merge engine** copies the template verbatim (UTF-8 BOM + CRLF
preserved), applies the plan with exact-match assertions, renames the program and
station display prefix, renumbers rungs, and keeps L5K/Decorated tag data (string
LEN rules included) in sync. Validation = XML/identifier/state checks **plus**
`validateAgainstDiagram` (every diagram action evidenced at its state; no device
commanded in a state whose node has no action for it). Failures at any stage are
fed back to the model, up to `JARVIS_MAX_ATTEMPTS` total calls.

**Model policy:** default `JARVIS_MODEL=claude-opus-5`; `claude-fable-5` is the
quality escalation path for hard stations (worth the price — quality wins);
`claude-sonnet-5` for cheap runs. Every result carries `meta.costEstimate`
computed from real usage tokens (cost is reported for visibility, never
optimized for at the expense of quality; `JARVIS_MAX_COST_USD` default 20 is a
sanity ceiling only).

- Version source of truth: `src/lib/agentGenerator/jarvisVersion.js` (`JARVIS_VERSION` + `HISTORY`)
- Every generated L5X program Description is stamped: `Generated by JARVIS v{version} ({date}) — SDC State Logic Builder`
- The `/api/generate` response includes `meta.jarvisVersion`
- Benchmarks are named by version — see `benchmarks/README.md` and `scripts/jarvisBenchmark.cjs`

Bump the version (and add a row here + a HISTORY entry in jarvisVersion.js) whenever
the pipeline's behavior changes: generation rules edits, template revisions, prompt
structure, validator checks, or repair-loop changes.

| Version | Date | Template Rev | Changes | Benchmark Result |
|---------|------|--------------|---------|------------------|
| 1.0.0 | 2026-08-19 | V4.2 | Initial Jarvis: V4.2 template law, prompt builder, validator, 2-round self-repair | FAIL — output truncated at 100K tokens after 14 min writing the full L5X; no importable file produced |
| 1.0.1 | 2026-08-19 | V4.2 | Surgical merge architecture: model authors edit plan only; deterministic template merge; diagram-vs-code cross-validation; IR module + .ir.txt; prompt = template extracts w/ cached stable prefix (no full L5X in context); default model claude-opus-5; 32K output cap; per-run costEstimate (v1.0.0 truncated at 100K writing full files) | SDC_Servo_PNP/SDC_Servo_PNP: **PASS**, 1 attempt, 307s, $0.94. 996-Magnet_Dial_AI/Magnet_Feed (pre-machineSpec IR): **PASS**, 3 attempts (2 truncated then compacted), 819s, $2.48. **Post-machineSpec re-run: FAIL** — spec intent grew the plan; attempts 1-2 truncated at the 32K cap, attempt 3 died on one ambiguous spliceRungs anchor (matched rungs 14 and 17), 3-attempt cap exhausted, $2.21, no file |
| 1.0.2 | 2026-08-19 | V4.2 | Fix release for machineSpec-scale plans: output cap 32K→64K, attempts 3→5 (caps must never degrade quality); plan continuation — a cleanly-parsed plan ending `"toBeContinued": true` triggers remainder requests, operations concatenated before merging; anchor disambiguation — optional `occurrence` (1-based) / `nearComment` (comment substring) on updateRung/spliceRungs; ambiguity errors now list every matched rung number AND its comment, passed verbatim to the repair round | 996-Magnet_Dial_AI/Magnet_Feed (machineSpec IR): **PASS**, 1 attempt, 0 repairs, 0 continuations, 490s, $1.44 (49,407 output tokens — would have truncated at the old 32K cap), 88 ops, 0 validation errors. Spec coordination survived: MagnetRetryCount retry counter (LES/GEQ 3 + escalation branch), p_MagnetPresented / p_RequestNextStack handshake tags, state-28/31 escalation rungs. 2 exitless-wait warnings on state 13 (per-rung check doesn't credit the sibling retry/escalation transitions that guarantee exit; no R20 alarm annunciates retries-exhausted) |
